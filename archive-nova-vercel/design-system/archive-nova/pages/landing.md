# Landing override

- Dark editorial presentation is intentional even when the app supports light mode.
- Goal: explain value before features, then convert to Explore or Writer.
- Hero motion may reach 6/10, but all content must be readable with motion disabled.
- Use the brand sparkle decoratively; use vector icons for feature/navigation semantics.
- Keep one primary CTA and one secondary CTA above the fold.

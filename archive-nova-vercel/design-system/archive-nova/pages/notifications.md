# Notifications override

- Notification type is conveyed by icon + text, never color alone.
- Unread state must remain visible with keyboard/high-contrast usage.
- Rows behave as one clear action target with a descriptive accessible label.
- Empty and signed-out states use the same shared system as other pages.
